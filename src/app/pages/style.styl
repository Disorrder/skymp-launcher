$root = "../../"

#app
    background url($root + "assets/images/bg_main.png")
    background-size cover
    background-position bottom center

.layout nav
    position absolute
    top 0
    right 0

    .nav-item
        padding 0.5rem
        color #aaa
        font-size 1.5em
